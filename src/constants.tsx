type States = {
  idle: string;
  loading: string;
  success: string;
  error: string;
};

export const requestStates = {
  idle: "IDLE",
  loading: "LOADING",
  success: "SUCCESS",
  error: "ERROR",
};

// 状態を管理
